# 🧠 SWARMICA v10.0

## Neural Field + Inverse Physics System

**Learned Continuum Swarm Dynamics | Self-Discovery of Physical Laws**

### Mathematical Framework

```

∂ρ/∂t = -∇·(ρv)
∂v/∂t = -∇E_θ(ρ) - γv + ν∇²v
E_θ(ρ) = tanh(mean(ρ)) + var(ρ) + θ·mean(ρ²)

```

### Key Innovation
- **Physics is learned, not given**
- **Energy functional is neural**
- **System discovers its own dynamics**

### Quick Start

```bash
cd v10.0
pip install -r requirements.txt
streamlit run streamlit/app.py
```

System Class

Neural PDEs | Energy-Based Models | Inverse Problems in Physics
